{
  "name": "@nge/ioc-core",
  "version": "0.1.0",
  "description": "The inversion of control interfaces used as the core of the module system.",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./node_modules && rm -rf ./dist",
    "build": "tslint . && tsc",
    "watch": "tsc -w"
  },
  "author": "Steve Hirscher",
  "license": "MIT",
  "repository": "https://github.com/shirscher",
  "dependencies": {},
  "devDependencies": {
    "@types/jasmine": "2.5.54",
    "@types/node": "8.0.11",
    "jasmine": "2.6.0",
    "typescript": "2.4.1"
  }
}
